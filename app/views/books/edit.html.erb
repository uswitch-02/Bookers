<h1>Editing Book</h1>
<p id="notice">Book was succesfully created.</p>
<% if @book.errors.any? %>
  <%= @book.errors.count %>1 errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @book do |f| %>

<h4>タイトル</h4>
<%= f.text_field :title %>

<h4>本文</h4>
<%= f.text_area :body %>
  
  <%= f.submit 'Update Book' %>
  
<% end %>
