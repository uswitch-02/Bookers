<h1>Books</h1>
<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th colspan="3"></th>
  </tr>
  <% @books.each do |book| %>
  <tr>
    <td><%= book.title%></td>
    <td><%= book.body%></td>
    <td></td>
    <td><%= link_to "Show", edit_book_path(book.id)%></td>
    <td><%= link_to "Edit" , book_path(book.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
    <td><%= link_to "Destroy", edit_book_path(book.id)%></td>
 </tr>
<% end %>
</table>

<h1>New book</h1>
<% if @book.errors.any? %>
  <%= @book.errors.count %>2 errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<table>
  <tr>
   <tb> <%= form_with model: @book, url: '/books', method: :post do |f| %></tb>
 <tb> <h4>Title</h4></tb>
  <tb><%= f.text_field :title %></tb>


  <tb><h4>Body</h4></tb>
  <tb><%= f.text_area :body %></tb>
   <tb> <p><%= f.submit 'create Book' %><p/></tb>
  </tr>
    <% end %>
</table>